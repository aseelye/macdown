# GitHub Style Generator

This tool generates the GitHub style from the official GitHub `primer-markdown` package using Dart Sass (`sass`). npm is required (to make use of `package-lock.json`). To update the style, bump the name in `Makefile`, and run:

```bash
npm install
npm update primer-markdown
make
```
